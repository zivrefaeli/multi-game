socket:
    - create a server with a full-duplex communication with its clients
    - enable server recive certain amount of clients, and handle them on different threads
    - on close server, send a closing-client to exit the blocking of 'accept'

tkinter:
    - create a server UI:
        - first, show the mechine IP address and add an input for the port
        - second, show the server info + closing button + connected clients list

    * check if client UI is required

pygame:
    - create an accelerated player
    - enable the player to shoot + crouch